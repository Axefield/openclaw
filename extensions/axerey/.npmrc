# npm configuration for Windows build tools
# This file configures npm/node-gyp to find MSBuild and Visual Studio tools

# Note: msvs_version and python are deprecated in newer npm versions
# They are set via environment variables instead (see setup-msbuild.ps1)
# For node-gyp, use environment variables: GYP_MSVS_VERSION and python in PATH

